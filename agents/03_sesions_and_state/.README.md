ADK WEB created sessions , state and runners for us 

A session has a state(store all sort of info & a dictionary)
-id
-app_name
-user_id
-last_update_time
eg user sadhana ko convo herna manlagyo with agent 
look at the session and allow bob to continue to chat with that session 

STATE     EVENT -> past record , msgs betwn us and the agent

1.in memory session : as i close the chat, all info from memory is gone
2.DatabaseSessionService : every convo is saved to a database 
3.VertexAISessionService : Google cloud